# =============================================================================
# FIRUMAN POS - EMAIL CONFIGURATION EXAMPLE
# =============================================================================
# Copy these settings to your .env file and update with your actual values

# -----------------------------------------------------------------------------
# MAIL CONFIGURATION
# -----------------------------------------------------------------------------
MAIL_MAILER=ses
MAIL_FROM_ADDRESS=noreply@yourdomain.com
MAIL_FROM_NAME="Firuman POS"

# -----------------------------------------------------------------------------
# AMAZON SES CONFIGURATION
# -----------------------------------------------------------------------------
# Get these from your AWS IAM Console
AWS_ACCESS_KEY_ID=your_access_key_here
AWS_SECRET_ACCESS_KEY=your_secret_access_key_here
AWS_DEFAULT_REGION=us-east-1
AWS_SES_REGION=us-east-1

# -----------------------------------------------------------------------------
# ALTERNATIVE MAIL PROVIDERS (if not using SES)
# -----------------------------------------------------------------------------

# SMTP Configuration (Generic)
# MAIL_MAILER=smtp
# MAIL_HOST=smtp.yourdomain.com
# MAIL_PORT=587
# MAIL_USERNAME=your_smtp_username
# MAIL_PASSWORD=your_smtp_password
# MAIL_ENCRYPTION=tls

# Mailgun Configuration
# MAIL_MAILER=mailgun
# MAILGUN_DOMAIN=your_mailgun_domain
# MAILGUN_SECRET=your_mailgun_secret

# Postmark Configuration
# MAIL_MAILER=postmark
# POSTMARK_TOKEN=your_postmark_token

# -----------------------------------------------------------------------------
# TESTING CONFIGURATION
# -----------------------------------------------------------------------------
# For development/testing, you can use log driver to see emails in logs
# MAIL_MAILER=log
# MAIL_LOG_CHANNEL=stack

# -----------------------------------------------------------------------------
# ADDITIONAL EMAIL SETTINGS
# -----------------------------------------------------------------------------
# Queue emails for better performance (optional)
QUEUE_CONNECTION=database

# Session configuration for email verification
SESSION_DRIVER=database
SESSION_LIFETIME=120

# -----------------------------------------------------------------------------
# NOTES:
# -----------------------------------------------------------------------------
# 1. Replace 'yourdomain.com' with your actual domain
# 2. Make sure your domain is verified in Amazon SES
# 3. If using SES, ensure you have production access (not sandbox mode)
# 4. Test your configuration with: php artisan email:check
# 5. Send test email with: php artisan email:test recipient@example.com
